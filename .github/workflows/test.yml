name: test

on: [pull_request]
  
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - env:
          JWT_KEY: ${{ secrets.JWT_KEY }}
      - run: npm install && npm run test:ci
