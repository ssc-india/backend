name: test

on: [pull_request]
  
jobs:
  test:
    runs-on: ubuntu-latest
    env:
      JWT_KEY: ${{ secrets }}
    steps:
      - name: Perfrom unit tests
        env:
          JWT_KEY: ${{ env.JWT_KEY }}
        run: echo $JWT_KEY
